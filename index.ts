// Q114
// iterating over amap student id name and log each pair to console
let students = new Map();
students.set(1, "sadaf");
students.set(2, "fatima");
students.set(3, "Ahmed");student
  console.log(`ID: ${id}, Name: ${name}`);
}
